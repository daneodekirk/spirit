extends main

block content
  h1 #{title}
  each post in posts
    p 
      a(href='/post/'+post._id) #{post.title} 
      | - #{post.slug}
      | - #{post._id}
      | - 
      a(href="/post/delete/?id="+post._id) Delete
